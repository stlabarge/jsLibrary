/* literals***************/

let days = ['mon', 'tue', 'wed', 'thur', 'fri'];

console.log(days.toString())
// this is a method to urn arrays into one string

console.log(days.length)

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley',
};
// objects are key value pairs, hich just means in an obect there are keys (what the name of the is part of the object is) and the value you for that key

console.log(soup.a)
//this is called a dot notation for n  object. this ihouw we can dig through anobject to gt back data we want

let car = "Toyota";
let dec = 10.2;

